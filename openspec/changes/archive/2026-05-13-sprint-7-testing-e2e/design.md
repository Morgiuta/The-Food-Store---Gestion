## Decisions

### Playwright over Cypress
Playwright es más moderno, rápido y tiene mejor DX para tests E2E en React + Vite.

## Setup

- Config en `frontend/playwright.config.ts`
- Tests en `frontend/tests/e2e/`
- Script: `npm run test:e2e`
